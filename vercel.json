{
    "rewrites": [
      { "source": "/(.*)", "destination": "/api/app" }
    ],
        "functions": {
            "/api/**/*.py": {
                "includeFiles": "/api/**/*.json"
            }
    }
}